<div class="container">
  <div class="jumbotron">
    <h1> Información de Sesión </h1>
    <table class="table">
      {{#each datos}}
        <tr>
          <th scope="row">{{this.name}}</th>
          <td>{{this.value}}</td>
        </tr>
      {{/each}}

    </table>
  </div>
</div>